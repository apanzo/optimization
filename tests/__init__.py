# so that unittests work from command line
# tests can be run from root (optimization) using **python -m unittest**
# or from tests (optimization/tests) using **python -m unittest discover -s .. -t ..**
